# SafeWork Frontend

This is a Vite + React + TypeScript starter scaffold created in the workspace.

Quick commands:

- Install: npm install
- Dev server: npm run dev
- Build: npm run build
- Typecheck: npm run typecheck
